class Cart {
    constructor() {
        this.products = [];
    }

    addProduct(prod) {
        this.products.push(product);
    }

    getProductCount() {
        return this.products.length;
    }
}